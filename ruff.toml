line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W"]
ignore = [
    "E501",  # line too long — not enforced in this project
]

[lint.per-file-ignores]
"backend/tests/*" = ["F811"]  # allow redefinition in test fixtures
